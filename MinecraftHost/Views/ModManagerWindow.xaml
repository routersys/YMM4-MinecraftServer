<Window x:Class="MinecraftHost.Views.ModManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MinecraftHost.ViewModels.Windows"
        xmlns:loc="clr-namespace:MinecraftHost.Localization"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{x:Static loc:Texts.Xaml_ModManager_Title}" Height="560" Width="680"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" 
        Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
        FontFamily="Segoe UI" FontSize="13"
        WindowStartupLocation="CenterOwner"
        AllowDrop="True"
        d:DataContext="{d:DesignInstance Type=vm:ModManagerViewModel}">

    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="ModListItem" TargetType="ListBoxItem">
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"/>
                <Setter Property="Padding" Value="12,9"/>
                <Setter Property="Opacity" Value="0.8"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Opacity" Value="1.0"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Opacity" Value="1.0"/>
                        <Setter Property="Foreground" Value="#60A0FF"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#005A9E" Padding="12,8"
                Visibility="{Binding IsServerRunning, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="{x:Static loc:Texts.Xaml_ModManager_ServerRunningNotice}"
                       Foreground="#C7E0F4" FontSize="12"/>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Padding="10,8"
                BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="{x:Static loc:Texts.Xaml_ModManager_Install}"
                        Command="{Binding InstallCommand}"
                        Style="{StaticResource BtnPrimary}"
                        Margin="0,0,6,0"/>
                <Button Content="{x:Static loc:Texts.Xaml_ModManager_OpenFolder}"
                        Command="{Binding OpenFolderCommand}"
                        Style="{StaticResource BtnSecondary}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <ListBox x:Name="ModList"
                     ItemsSource="{Binding Mods}"
                     SelectedItem="{Binding SelectedMod, Mode=TwoWay}"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="0"
                     ItemContainerStyle="{StaticResource ModListItem}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Rectangle Grid.Column="0" Width="6" Height="6"
                                       Fill="#60A0FF" Margin="0,0,10,0"
                                       VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Name}"
                                       FontWeight="SemiBold"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"/>
                            <TextBlock Grid.Column="2"
                                       Text="{Binding SizeKB, StringFormat={}{0} KB}"
                                       Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                       FontSize="11"
                                       VerticalAlignment="Center"
                                       Margin="12,0,0,0"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Background="Transparent"
                    IsHitTestVisible="False"
                    Visibility="{Binding HasNoMods, Converter={StaticResource BoolToVis}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="{x:Static loc:Texts.Xaml_ModManager_NoMods}"
                               Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" FontSize="15" Opacity="0.8"
                               HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="{x:Static loc:Texts.Xaml_ModManager_DropOrInstallHint}"
                               Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" FontSize="11"
                               HorizontalAlignment="Center" TextWrapping="Wrap"
                               TextAlignment="Center" MaxWidth="320"/>
                </StackPanel>
            </Border>

            <Border x:Name="DropOverlay"
                    Background="#1A3A5A"
                    BorderBrush="#60A0FF"
                    BorderThickness="3"
                    Visibility="Collapsed"
                    IsHitTestVisible="False">
                <TextBlock Text="{x:Static loc:Texts.Xaml_ModManager_DropToInstall}"
                           Foreground="#60A0FF"
                           FontSize="18" FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>
        </Grid>

        <Border Grid.Row="3" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Padding="12,8"
                BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Content="{x:Static loc:Texts.Xaml_ModManager_Uninstall}"
                        Command="{Binding UninstallCommand}"
                        Style="{StaticResource BtnDanger}"/>
                <TextBlock Grid.Column="2"
                           Text="{Binding StatusMessage}"
                           Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" FontSize="12"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>